# GitHub Configuration

This folder contains repository automation that runs on GitHub.

## Structure

```txt
workflows/
  GitHub Actions workflow files
```

Keep GitHub-only configuration here. Runtime demo source belongs under `src/`,
and local helper scripts belong under `scripts/`.
