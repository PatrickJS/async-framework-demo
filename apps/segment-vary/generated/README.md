# Generated Segment Vary Files

This generated folder uses the shared product model split but emphasizes `SegmentModel`.

The model output varies by `segment=free|pro`; cache keys include that model output so the demo does not reuse free pricing for pro pricing or the reverse.

