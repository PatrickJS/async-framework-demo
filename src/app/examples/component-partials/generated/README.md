# Generated Partial Files

These generated files use the same model/controller/template shape as the product cache example.

The point of this folder is the payload, not a different component. The framework returns rendered HTML plus a resume context envelope to show the metadata a real partial system would need.
