# Generated Personalized Pending Files

This generated folder uses the same product card split but the app defaults to pending SSR output.

The folder is here to show that the generated component can stay simple while server-only policy decides whether resolved HTML or pending HTML should be sent first.

