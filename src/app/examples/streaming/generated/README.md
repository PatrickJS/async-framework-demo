# Generated Streaming Files

This generated folder uses readable files instead of hash names.

For this example the generated code is the same component split as `product-cache`; the difference is the framework render mode. That is intentional: the authoring and generated component can stay the same while the runtime chooses a streaming strategy.
